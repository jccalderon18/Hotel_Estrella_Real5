@model HotelEstrellaReal5.Models.Usuario

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<div class="col-lg-9">
    <div class="card-box">

        <h4 class="header-title m-b-30 text-center">Eliminar usuario</h4>
        <hr />
        <address>
            <dl class="row">
                <dt class="col-sm-4">
                    @Html.DisplayNameFor(model => model.IdUsuario)
                </dt>
                <dd class="col-sm-8">
                    @Html.DisplayFor(model => model.IdUsuario)
                </dd>
                <dt class="col-sm-4">
                    @Html.DisplayNameFor(model => model.NombreCompleto)
                </dt>
                <dd class="col-sm-8">
                    @Html.DisplayFor(model => model.NombreCompleto)
                </dd>
                <dt class="col-sm-4">
                    @Html.DisplayNameFor(model => model.Email)
                </dt>
                <dd class="col-sm-8">
                    @Html.DisplayFor(model => model.Email)
                </dd>
                <dt class="col-sm-4">
                    @Html.DisplayNameFor(model => model.Clave)
                </dt>
                <dd class="col-sm-8">
                    @Html.DisplayFor(model => model.Clave)
                </dd>
                <dt class="col-sm-4">
                    @Html.DisplayNameFor(model => model.ConfirmarClave)
                </dt>
                <dd class="col-sm-8">
                    @Html.DisplayFor(model => model.ConfirmarClave)
                </dd>
                <dt class="col-sm-4">
                    @Html.DisplayNameFor(model => model.FotoPerfilUrl)
                </dt>
                <dd class="col-sm-8">
                    @Html.DisplayFor(model => model.FotoPerfilUrl)
                </dd>
                <dt class="col-sm-4">
                    @Html.DisplayNameFor(model => model.IdRolNavigation)
                </dt>
                <dd class="col-sm-8">
                    @Html.DisplayFor(model => model.IdRolNavigation.Nombre)
                </dd>
                <dt class="col-sm-4">
                    @Html.DisplayNameFor(model => model.FechaRegistro)
                </dt>
                <dd class="col-sm-8">
                    @Html.DisplayFor(model => model.FechaRegistro)
                </dd>
            </dl>
        </address>

        <address class="text-center">
            <form asp-action="Delete" id="formulario" onsubmit="enviarformulario(event)">
                <input type="hidden" asp-for="IdUsuario" />
                <button type="submit" class="btn btn-danger waves-effect waves-light m-l-5">Eliminar</button>
                <a asp-action="Index" class="btn btn-secondary waves-effect waves-light m-l-5">Salir</a>
            </form>
        </address>

    </div>

</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        function enviarformulario(event) {
            event.preventDefault();

            swal.fire({
                title: '¿Estás seguro que quieres eliminar este usuario?',
                icon: 'warning',
                showCancelButton: true,
                allowOutsideClick: false,
                confirmButtonText: 'si'
            }).then((resultado) => {
                if (resultado.isConfirmed) {
                    const formulario = document.getElementById('formulario');
                    formulario.submit();
                }
            })
        }
    </script>
}
